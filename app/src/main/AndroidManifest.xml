<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/kalozteka"
        android:label="@string/app_name"
        android:roundIcon="@drawable/kalozteka"
        android:supportsRtl="true"
        android:theme="@style/Theme.KalozTeka"
        tools:targetApi="31">

        <activity
            android:name=".UjvideoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.RegActivity"
            android:exported="false"
            android:label="@string/title_activity_reg"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:resizeableActivity="true"
            android:theme="@style/Theme.KalozTeka.NoActionBar"
            android:screenOrientation="portrait"
            tools:targetApi="24">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VideoDetailActivity" />
    </application>

</manifest>